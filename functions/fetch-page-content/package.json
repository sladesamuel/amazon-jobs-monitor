{
  "name": "fetch-page-content",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "postbuild": "cp package.json dist/ && (cd dist; yarn install --prod)"
  },
  "devDependencies": {
    "@types/node": "^20.9.2",
    "typescript": "^5.2.2"
  },
  "dependencies": {
    "axios": "^1.6.2"
  }
}
